---
applications:
- name: cf-rails
  memory: 1G
  instances: 1
  command: rake db:migrate && script/server
  host: cf-rails
  domain: cfapps.io
  path: .
  services:
    cleardb-cf-rails:
      label: cleardb
      provider: cleardb
      version: n/a
      plan: spark
